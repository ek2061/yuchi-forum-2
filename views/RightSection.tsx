import { Box } from "@chakra-ui/react";

export const RightSection = () => {
  return (
    <Box background="blue.100" display={{ base: "none", lg: "block" }} w={208}>
      RightSection
    </Box>
  );
};
